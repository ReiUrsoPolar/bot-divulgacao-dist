# .npmrc — sem build_from_source global (quebra o sharp/baileys no Linux)
# O better-sqlite3 v12 usa node-gyp directamente e compila sempre do source.
# O start.sh garante o rebuild explícito após npm install.
