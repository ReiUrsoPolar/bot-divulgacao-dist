node_modules/
auth_info_baileys/
session/
database/
*.sqlite
*.db
config/bot.json
*.jsonl
.tmp/

# Variáveis de ambiente do criador — nunca commitar
.env

# Output do build/obfuscação
dist/

# Cópias de segurança do config: são do dono da máquina, nunca do repositório.
config/*.bak
config/bot.json.bak
